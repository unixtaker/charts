apiVersion: v1
kind: Service
metadata:
  name: {{ include "whisparr.fullname" . }}
  labels:
    {{- include "whisparr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whisparr.selectorLabels" . | nindent 4 }}
