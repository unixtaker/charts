apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "whisparr.fullname" . }}-test-connection"
  labels:
    {{- include "whisparr.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "whisparr.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
